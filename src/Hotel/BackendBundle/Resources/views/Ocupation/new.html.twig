{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ocupation creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('backend_ocupations') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
